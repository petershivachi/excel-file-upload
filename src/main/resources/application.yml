server:
  port: 9000

spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/file_upload
    username: root
    password: midnighttrain
    driverClassName: org.mariadb.jdbc.Driver

  servlet:
    multipart:
      max-file-size: 2MB
      max-request-size: 2MB

  jpa:
    hibernate:
      ddlAuto:  update

